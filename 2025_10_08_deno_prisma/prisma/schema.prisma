datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
    output   = "client"
    runtime  = "deno"
}

model user {
    id   String @id @default(cuid())
    name String
}
